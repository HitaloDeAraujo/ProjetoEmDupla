@model IEnumerable<ProjetoAPIEmDupla.Models.OrdemServico>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DataContratacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataExecucao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DataContratacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataExecucao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdOS }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdOS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdOS })
        </td>
    </tr>
}

</table>
